# URL of the backend API. Required when the frontend and backend are deployed
# to different origins (e.g. separate hosting services). Defaults to
# http://localhost:5001 for local development if unset.
REACT_APP_API_URL=http://localhost:5001
